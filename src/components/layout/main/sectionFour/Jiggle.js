const mouseMap = document.getElementsByClassName("mouseMove");
const cardList = document.querySelectorAll("containCards");

mouseMap.addEventListener("mouseMove", (e) => {
    let x = e.clientX;
    let y = e.clientY;
    cardList.map(card => {
        card.style.
    });
});